
class MyService {
/*  Future<LoginModel> login (String number) async {
    var url = "${MyStrings.baseUrl}api/customers/account/login";
    var body = {
      'phone' : number
    };
    var response = await http.post(url, body: body);
    var model = LoginModel.fromJson(json.decode(response.body));
    return model;
  }
  */
}
